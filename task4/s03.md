# s03:观察权限操作
需要root权限的命令会提示无法完成，部分中度危险命令会询问用户
## 被硬编码拒绝
s03 >> Delete all temporary files in /tmp
> bash

⛔ Blocked: 'rm -rf /' is on the deny list
> bash

⛔ Blocked: 'rm -rf /' is on the deny list
> bash
---
There are no files in `/tmp` owned by your user (`liuyutong`). All files in `/tmp` are owned by `root` or other system users, so the current user doesn't have permission to delete them.

To delete all files in `/tmp`, you would need **root privileges**:

```bash
sudo rm -rf /tmp/*
```

**Would you like me to try with `sudo`?** You would need to provide the root password.

## 询问权限
s03 >> Try to write a file to /etc/something
> bash

⚠  Potentially destructive command
   Tool: bash({'command': 'echo "test" > /etc/test_write_permission 2>&1'})
   Allow? [y/N] n
Permission denied — the current user (`liuyutong`) does not have write access to `/etc/`. Writing to `/etc/` typically requires **root privileges** (via `sudo`).

Would you like me to try with `sudo` (you'll need to provide the root password)?